<?xml version="1.0" encoding="UTF-8"?>
<openerp>
    <data>
        <!-- Races -->
        <record id="action_races" model="ir.actions.act_window">
            <field name="name">Races</field>
            <field name="res_model">trotticont.race</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
        </record>

        <menuitem name="Races" id="menu_races" sequence="5" action="action_races" parent="menu_trotticont"/>

        <!-- Teams -->
        <record id="action_teams" model="ir.actions.act_window">
            <field name="name">Teams</field>
            <field name="res_model">trotticont.team</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
        </record>

        <menuitem name="Teams" id="menu_teams" sequence="10" action="action_teams" parent="menu_trotticont"/>

        <!-- Categories -->
        <record id="action_team_categories" model="ir.actions.act_window">
            <field name="name">Categories</field>
            <field name="res_model">trotticont.team_category</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
        </record>

        <menuitem name="Team categories" id="menu_team_categories" sequence="5" action="action_team_categories" parent="menu_trotticont"/>

        <!-- Groups -->
        <record id="action_team_groups" model="ir.actions.act_window">
            <field name="name">Groups</field>
            <field name="res_model">trotticont.team_group</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
        </record>

        <menuitem name="Team groups" id="menu_team_groups" sequence="5" action="action_team_groups" parent="menu_trotticont"/>

        <!-- App Menu -->
        <menuitem name="Trotticont"
            id="menu_trotticont"
            sequence="5" action="" web_icon="trotticonts,static/description/menu.png" />
    </data>
</openerp>